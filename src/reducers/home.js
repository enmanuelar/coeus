import * as actions from '../actions/home';

const initialState = {

};

const home = (action, currentState = initialState) => {
  switch (action.type){
    default:
      return currentState;
  }
};

export default home;
